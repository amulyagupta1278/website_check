[build]
  base    = "frontend"
  publish = "build"
  command = "npm ci --legacy-peer-deps && npm run build"

[build.environment]
  NODE_VERSION = "20"

# SPA redirect so refresh/deep links don’t 404
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
